[build]
builder = "DOCKERFILE"
dockerfilePath = "serving/Dockerfile"
watchPatterns = ["serving/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
